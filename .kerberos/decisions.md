# CR Immigration decisions

Only record decisions that future tasks must preserve.

## Active decisions

- Keep secrets outside the repository and never copy secret values into Kerberos files.
- Keep task history out of durable context files.
