# Local development defaults to console logging. Set `neon` after provisioning
# Neon through the Vercel Marketplace and pulling its environment variables.
INTAKE_STORE=console
DATABASE_URL=postgresql://user:password@host/database?sslmode=require
