# Tasks

Create one concise file per substantial task when durable handoff context is useful. Include scope, acceptance criteria, relevant paths, decisions, validation, and remaining work. Do not store secrets or transient command logs.

